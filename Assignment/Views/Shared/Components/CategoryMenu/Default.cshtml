@model IEnumerable<Category>

<li class="dropdown">
    <a asp-controller="Books" asp-action="ListAll"
       class="dropdown-toggle" data-toggle="dropdown">Category<b class="caret"></b></a>
    <ul class="dropdown-menu">
        @foreach (var category in Model)
        {
            <li>
                <a asp-controller="Books" asp-action="ListCategory"
                   asp-route-categoryName="@category.Name">@category.Name</a>
            </li>
        }
        <li class="divider"></li>
        <li>
            <a asp-controller="Books" asp-action="ListAll" asp-route-category="">View all books</a>
        </li>
    </ul>
</li>
